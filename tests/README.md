# Integration tests

M00 lifecycle and version tests live beside their pure modules. Add cross-module integration tests here as storage, search, and Windows adapters are introduced. Interactive tray, focus, IME, DPI, and Explorer-restart checks remain in the owner-executed manual acceptance plan because GitHub-hosted Windows runners do not provide a reliable interactive desktop session.
