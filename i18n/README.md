# Localization resources

M00 keeps its small architecture-shell strings in `ui/app-window.slint`. M03 must move every user-visible string into paired `zh-CN` and `en-US` resources and add key-parity tests before the production search UI is accepted.
